#ifdef SET_REFLECTIVENESS
	#define REFLECTIVENESS(v) reflectiveness = v;
#else
	#define REFLECTIVENESS(v)
#endif
#ifdef SET_SPECULARNESS
	#define SPECULARNESS(v) specularness = v;
#else
	#define SPECULARNESS(v)
#endif
#ifdef SET_VOXEL_ID
	#define VOXEL_ID(v) voxelId = v;
#else
	#define VOXEL_ID(v)
#endif

if (materialId < 1900) {
	
} else {
	
}
